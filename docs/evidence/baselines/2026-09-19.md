# Baseline Evidence — 2026-09-19

This record is an engineering baseline, not release approval. It establishes the commands and evidence locations that every release candidate must refresh.

| Area | Command | Result | Evidence |
| --- | --- | --- | --- |
| API container preflight | `./scripts/test-preflight.sh` | Passed: Docker server 29.5.3 reachable. | Terminal output recorded with commit `b0276f8`. |
| API integration suite | `cd apps/api && ./mvnw test` | Passed on clean commit `b0276f8`: 155 tests, 0 failures, 0 errors, 0 skipped; 27.679 s. | Surefire XML: `apps/api/target/surefire-reports/` |
| Web unit suite | `cd apps/web && npm test -- --watch=false` | Passed: 8 files, 20 tests. | Terminal output recorded with commit `b0276f8`. |
| Web production build | `cd apps/web && npm run build` | Passed: 566.54 kB raw initial bundle, 127.17 kB estimated transfer. | Terminal output recorded with commit `b0276f8`. |
| Portfolio build/test/type check | `cd ../../portfolio-site && npm run build && npm test && npm run lint` | Passed: production build (169.06 kB raw initial bundle), 1 build test, and TypeScript check. | Terminal output recorded with commit `b0276f8`. |

## Known environment note

On the baseline machine, Docker Desktop reports an arm64 server while `postgis/postgis:18-3.6` reports amd64. Testcontainers warns that emulation can make the suite slower. This warning does not justify excluding integration tests; measure it in each candidate if it affects a performance budget.

## Scope of the verified API result

The API result above was executed from an isolated clean Git worktree at commit `b0276f8`. The primary developer worktree contained unrelated, pre-existing edits to `GroupController.java`; those edits were deliberately not included in the baseline or modified by this work.
