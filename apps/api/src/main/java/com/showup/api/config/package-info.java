/**
 * Spring {@code @Configuration}: the stateless bearer-token filter chain, the JWT encoder and
 * decoder, the password encoder, and the {@code @CurrentMember} argument resolver registration.
 */
package com.showup.api.config;
