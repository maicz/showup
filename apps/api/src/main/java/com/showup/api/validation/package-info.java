/**
 * Custom {@code jakarta.validation} constraints and their validators — the cross-field rules no
 * single-field annotation can express. See docs/domain-model.md#requests.
 */
package com.showup.api.validation;
