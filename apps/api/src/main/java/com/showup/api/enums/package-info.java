/**
 * Enumerations shared by entities and DTOs — statuses, roles, formats, visibilities. They live
 * apart from {@link com.showup.api.entity} because both layers name them and neither owns them.
 * ({@code enum} is a reserved word, hence the plural package name.)
 */
package com.showup.api.enums;
