/**
 * Spring Data JPA repositories — one per aggregate root, plus the join tables that are queried
 * directly. Query methods only; business rules belong in {@link com.showup.api.service}.
 */
package com.showup.api.repository;
